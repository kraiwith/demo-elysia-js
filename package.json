{
  "name": "demoElysiaJs",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "build:dev": "bun build src/index.ts --env-file=.env",
    "build": "bun build src/index.ts --env-file=.env.prod"
  },
  "dependencies": {
    "@elysiajs/swagger": "^1.1.1",
    "@prisma/client": "^5.18.0",
    "elysia": "latest"
  },
  "devDependencies": {
    "bun-types": "latest",
    "prisma": "^5.18.0"
  },
  "module": "src/index.js"
}
